# ==============================================================================
# NoFluxo UNB - Requisitos Consolidados do Projeto Python
# ==============================================================================
# Para instalar tudo de uma vez no ambiente virtual (recomendado):
#   python -m pip install -r requirements.txt
#
# Para instalar apenas dependências de um módulo específico:
#   DBA / Banco:   python -m pip install -r DBA/database/requirements.txt
#   Scraping:      python -m pip install -r DBA/scraping/requirements.txt
#   AI Agent:      python -m pip install -r mcp_agent/requirements.txt
# ==============================================================================

# --- DBA / Banco de Dados (DBA/database) ---
supabase>=2.24.0
tenacity>=8.2.0
python-dotenv>=1.2.2

# --- Scraping e Coleta de Dados (DBA/scraping) ---
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.33.0
tqdm>=4.66.0
pandas>=2.0.0

# --- Processamento de PDF e OCR (Parse PDF) ---
Flask>=3.1.3
Flask-Cors>=6.0.0
pypdf>=4.0.0
Pillow>=12.3.0
pdf2image>=1.16.0
pytesseract>=0.3.10

# --- Agente de IA / MCP (mcp_agent) ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0,<3.0.0
openai>=1.60.0
google-generativeai>=0.8.0

# --- Testes e Qualidade (DBA/tests) ---
pytest>=7.4.0
pytest-cov>=4.1.0